import { Context } from '@nuxt/types/app'

export default function (ctx: Context) {
  // console.log(ctx.beforeNuxtRender((params) => {}))
  // ctx.app.router?.((to, from, next) => {
  //   const componet = ctx.app.router?.getMatchedComponents(to)
  //   if (!componet) {
  //     return
  //   }
  //   console.log(componet[0])
  // })
}
